import { GoalDisplayHoursMinutesPipe } from './goal-display-hours-minutes.pipe';


describe('GoalDisplayHoursMinutesPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalDisplayHoursMinutesPipe();
    expect(pipe).toBeTruthy();
  });
});

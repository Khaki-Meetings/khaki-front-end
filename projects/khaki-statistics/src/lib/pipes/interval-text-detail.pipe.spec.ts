import { IntervalTextDetailPipe } from './interval-text-detail.pipe';

describe('IntervalTextDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new IntervalTextDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
